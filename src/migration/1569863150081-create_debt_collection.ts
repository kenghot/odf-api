import { MigrationInterface, QueryRunner } from "typeorm";

export class createDebtCollection1569863150081 implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<any> {
    await queryRunner.query(
      "CREATE TABLE `debt_collection_letters` (`id` bigint NOT NULL AUTO_INCREMENT, `createdDate` datetime(6) NULL COMMENT 'วันที่สร้าง' DEFAULT CURRENT_TIMESTAMP(6), `updatedDate` datetime(6) NULL COMMENT 'วันที่ปรับปรุง' DEFAULT CURRENT_TIMESTAMP(6), `createdBy` int NULL COMMENT 'รหัสผู้ใช้ที่สร้าง', `createdByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่สร้าง' DEFAULT '', `updatedBy` int NULL COMMENT 'รหัสผู้ใช้ที่ปรับปรุงล่าสุด', `updatedByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่ปรับปรุงล่าสุด' DEFAULT '', `debtCollectionId` bigint NOT NULL COMMENT 'รายการติดตามทวงหนี้', `letterType` varchar(255) NOT NULL COMMENT 'ประเภทจดหมาย \"CLB\" = หนังสือทวงถามผูักู้ , \"CLG\" = หนังสือทวงถามผู้ค้้ำ, \"CCB\" = หนังสือบอกเลิกสัญญาผู้กู้ , \"CCG\" = หนังสือบอกเลิกสัญญาผู้ค้ำ, \"CSH\" = หนังสือสืบหาทายาท, \"CSM\" = หนังสือสืบหาผู้จัดการมรดก, \"CLR\" = หนังสือแจ้งทายาท/ผู้จัดการมรดก', `documentDate` date NULL COMMENT 'วันที่ทำหนังสือแจ้ง', `postDate` date NULL COMMENT 'วันที่จัดส่งหนังสือแจ้ง', `isSentBack` tinyint NOT NULL COMMENT 'จดหมายถูกตีกลับ' DEFAULT 0, `sentBackReasonType` int NULL COMMENT 'สาเหตุที่ถูกตีกลับ', `sentBackReasonTypeDescription` varchar(255) NULL COMMENT 'สาเหตุที่ถูกตีกลับ อื่นๆ', `isCollectable` tinyint NOT NULL COMMENT 'ติดต่อชำระเงิน / ไม่ติดต่อชำระเงิน' DEFAULT 0, PRIMARY KEY (`id`)) ENGINE=InnoDB"
    );
    await queryRunner.query(
      "CREATE TABLE `debt_collection_visits` (`id` bigint NOT NULL AUTO_INCREMENT, `createdDate` datetime(6) NULL COMMENT 'วันที่สร้าง' DEFAULT CURRENT_TIMESTAMP(6), `updatedDate` datetime(6) NULL COMMENT 'วันที่ปรับปรุง' DEFAULT CURRENT_TIMESTAMP(6), `createdBy` int NULL COMMENT 'รหัสผู้ใช้ที่สร้าง', `createdByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่สร้าง' DEFAULT '', `updatedBy` int NULL COMMENT 'รหัสผู้ใช้ที่ปรับปรุงล่าสุด', `updatedByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่ปรับปรุงล่าสุด' DEFAULT '', `debtCollectionId` bigint NOT NULL COMMENT 'รายการติดตามทวงหนี้', `visitDate` date NULL COMMENT 'วันที่สัมภาษณ์', `visitType` varchar(255) NOT NULL COMMENT 'รูปแบบการเข้าเยี่ยม: \"DCB\" = debt collection bollower เข้าติดตามทวงถามผู้กู้, \"DCG\" = debt collection gurantor เข้าติดตามทวงถามผู้ค้ำ', `isMeetTarget` tinyint NOT NULL COMMENT 'พบ / ไม่พบ' DEFAULT 0, `overdueReasons` varchar(128) NOT NULL COMMENT 'คำอธิบาย สาเหตุที่ไม่ชำระเงิน' DEFAULT '', `dismissReason` varchar(128) NOT NULL COMMENT 'คำอธิบาย สาเหตุที่ไม่พบ' DEFAULT '', `isWorking` tinyint NOT NULL COMMENT 'สถานะการประกอบอาชีพ  ทำงาน/ไม่ทำงาน', `hasExtraIncome` tinyint NOT NULL COMMENT 'มีีรายได้อื่นๆ', `extraIncome` decimal(10,2) NOT NULL COMMENT 'รายได้อื่นๆ' DEFAULT 0, `extraIncomeDescription` varchar(128) NOT NULL COMMENT 'คำอธิบาย รายได้อื่นๆ' DEFAULT '', `familyMember` int NOT NULL COMMENT 'สมาชิกในครอบครัว', `familyMemberDescription` varchar(128) NOT NULL COMMENT 'คำอธิบาย สมาชิกในครอบครัว' DEFAULT '', `expenseDeclaration` varchar(128) NOT NULL COMMENT 'ภาระที่ต้องรับผิดชอบค่าใช้จ่าย' DEFAULT '', `problem1` varchar(256) NOT NULL COMMENT 'ปัญหาด้านสุขภาพ' DEFAULT '', `problem2` varchar(256) NOT NULL COMMENT 'ปัญหาด้านการประกอบอาชีพ' DEFAULT '', `problem3` varchar(256) NOT NULL COMMENT 'ปัญหาด้านอื่นๆ' DEFAULT '', `inspection1` varchar(256) NOT NULL COMMENT 'การสืบหาฯ-ภาระหนี้สิน' DEFAULT '', `inspection2` varchar(256) NOT NULL COMMENT 'การสืบหาฯ-ทรัพย์สินของผู้กู้ยืม' DEFAULT '', `inspection3` varchar(256) NOT NULL COMMENT 'การสืบหาฯ-สิทธิเรียกร้องจากบุคคลภายนอก' DEFAULT '', `visitorName` varchar(255) NOT NULL COMMENT 'ชื่อผู้สัมภาษณ์' DEFAULT '', `visitorPosition` varchar(255) NOT NULL COMMENT 'ตำแหน่งผู้สัมภาษณ์' DEFAULT '', `comments` varchar(255) NOT NULL COMMENT 'ข้อสังเกตของเจ้าหน้าที่เกี่ยวกับผู้กู้ยืม' DEFAULT '', `currentAddressHouseNo` varchar(128) NOT NULL COMMENT 'เลขที่บ้าน' DEFAULT '', `currentAddressBuildingName` varchar(128) NOT NULL COMMENT 'ชื่ออาคาร หมู่บ้าน' DEFAULT '', `currentAddressRoomNo` varchar(128) NOT NULL COMMENT 'ห้อง' DEFAULT '', `currentAddressFloor` varchar(128) NOT NULL COMMENT 'ชั้น' DEFAULT '', `currentAddressHmoo` varchar(128) NOT NULL COMMENT 'หมู่ที่' DEFAULT '', `currentAddressSoi` varchar(128) NOT NULL COMMENT 'ซอย' DEFAULT '', `currentAddressStreet` varchar(128) NOT NULL COMMENT 'ถนน' DEFAULT '', `currentAddressSubDistrictCode` varchar(8) NOT NULL COMMENT 'รหัสตำบล' DEFAULT '', `currentAddressSubDistrict` varchar(128) NOT NULL COMMENT 'ตำบล' DEFAULT '', `currentAddressDistrictCode` varchar(8) NOT NULL COMMENT 'รหัสอำเภอ' DEFAULT '', `currentAddressDistrict` varchar(128) NOT NULL COMMENT 'อำเภอ' DEFAULT '', `currentAddressProvinceCode` varchar(8) NOT NULL COMMENT 'รหัสจังหวัด' DEFAULT '', `currentAddressProvince` varchar(128) NOT NULL COMMENT 'จังหวัด' DEFAULT '', `currentAddressZipcode` varchar(8) NOT NULL COMMENT 'รหัสไปรษณีย์' DEFAULT '', `currentAddressLatitude` varchar(128) NOT NULL COMMENT 'ละติจูด' DEFAULT '', `currentAddressLongitude` varchar(128) NOT NULL COMMENT 'ลองจิจูด' DEFAULT '', `occupationId` varchar(255) NULL COMMENT 'รหัสอาชีพ', `occupationName` varchar(128) NOT NULL COMMENT ' ชื่ออาชีพ' DEFAULT '', `occupationDescription` varchar(128) NOT NULL COMMENT 'คำอธิบาย' DEFAULT '', `occupationSalary` decimal(10,2) NOT NULL COMMENT 'เงินเดือน' DEFAULT 0, PRIMARY KEY (`id`)) ENGINE=InnoDB"
    );
    await queryRunner.query(
      "CREATE TABLE `debt_collections` (`id` bigint NOT NULL AUTO_INCREMENT, `createdDate` datetime(6) NULL COMMENT 'วันที่สร้าง' DEFAULT CURRENT_TIMESTAMP(6), `updatedDate` datetime(6) NULL COMMENT 'วันที่ปรับปรุง' DEFAULT CURRENT_TIMESTAMP(6), `createdBy` int NULL COMMENT 'รหัสผู้ใช้ที่สร้าง', `createdByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่สร้าง' DEFAULT '', `updatedBy` int NULL COMMENT 'รหัสผู้ใช้ที่ปรับปรุงล่าสุด', `updatedByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่ปรับปรุงล่าสุด' DEFAULT '', `accountReceivableId` bigint NOT NULL COMMENT 'บัญชีลูกหนี้', `prescriptionStartDate` date NULL COMMENT 'วันที่เริ่มนับอายุความ', `prescriptionValue` int NOT NULL COMMENT 'อายุความ' DEFAULT 0, `prescriptionUnit` varchar(255) NOT NULL COMMENT 'หน่วยนับอายุความ' DEFAULT 'YEAR', `interruptReason` varchar(255) NULL COMMENT 'สาเหตุการสะดุดอายุความ : \"P\" = ชำระเงิน {paid} , \"AD\" = รับสภาพหนี้ {ackOfDebt} , \"D\" = ผู้กู้เสียชีวิต {dead}, \"AF\" = รับสภาพความผิดในกรณีหมดอายุความไปแล้ว {ackAfter}', `comments` varchar(255) NULL COMMENT 'หมายเหตุ', `step` int NOT NULL COMMENT 'ขั้นตอนการติดตามล่าสุด' DEFAULT 0, `active` tinyint NOT NULL COMMENT 'สถานะบ่งบอกว่าเป็นรายการติดตามหนี้ล่าสุด' DEFAULT 0, `debtAcknowledgementIsAcknowledge` tinyint NOT NULL COMMENT 'ยืนยันการรับสภาพหนี้' DEFAULT 0, `debtAcknowledgementIdCardNo` varchar(13) NOT NULL COMMENT 'หมายเลขบัตรประชาชนผู้รับสภาพหนี้' DEFAULT '', `debtAcknowledgementTitle` varchar(128) NOT NULL COMMENT 'คำนำหน้าชื่อผู้รับสภาพหนี้' DEFAULT '', `debtAcknowledgementFirstname` varchar(128) NOT NULL COMMENT 'ชื่อผู้รับสภาพหนี้' DEFAULT '', `debtAcknowledgementLastname` varchar(128) NOT NULL COMMENT 'นามสกุลผู้รับสภาพหนี้' DEFAULT '', `debtAcknowledgementTelephone` varchar(64) NOT NULL COMMENT 'หมายเลขโทรศัพท์ผู้รับสภาพหนี้' DEFAULT '', `debtAcknowledgementIsBehalf` tinyint NOT NULL COMMENT 'เป็นตัวแทนของผู้กู้' DEFAULT 0, `debtAcknowledgementLocation` varchar(255) NOT NULL COMMENT 'สถานที่รับสภาพหนี้' DEFAULT '', `debtAcknowledgementAcknowledgeDate` date NULL COMMENT 'วันที่รับสภาพหนี้', `debtAcknowledgementBirthDate` date NULL COMMENT 'วันเกิด >> หากไม่ทราบวันเกิดจะระบุเป็นวันที่  1/1/YYYY', `debtAcknowledgementIsOnlyBirthYear` tinyint NOT NULL COMMENT 'ไม่ทราบวันเกิด' DEFAULT 0, `debtAcknowledgementOnBehalfOf` varchar(64) NOT NULL COMMENT 'ในฐานะผู้จัดการมรดกหรือทายาทของ' DEFAULT '', `debtAcknowledgementOutstandingDebtBalance` decimal(10,2) NOT NULL COMMENT 'ยอดคงเหลือล่าสุด (หนี้คงค้าง)' DEFAULT 0, `debtAcknowledgementInstallmentAmount` decimal(10,2) NOT NULL COMMENT 'จำนวนเงินชำระต่องวด' DEFAULT 0, `debtSueIsApprovedSue` tinyint NOT NULL COMMENT 'อนุมัติฟ้อง' DEFAULT 0, `debtSueSubmitDate` date NULL COMMENT 'วันที่ยืนฟ้องศาล', `debtSueJudgementDate` date NULL COMMENT 'วันที่ศาลพิพากษา', `debtSueDebtAmount` decimal(10,2) NOT NULL COMMENT 'เงินต้นที่ส่งฟ้อง' DEFAULT 0, `debtSueInterestStartDate` date NULL COMMENT 'วันที่เริ่มคิดดอกเบี๊ย', `debtSueInterestEndDate` date NULL COMMENT 'วันที่สิ้นสุดการคิดดอกเบี๊ย', `debtSueInterestRate` decimal(10,2) NOT NULL COMMENT 'ดอกเบี้ย %' DEFAULT 0, `debtSueInterestAmount` decimal(10,2) NOT NULL COMMENT 'ดอกเบี้ยทั้งหมด ณ วันที่กำหนด (default = today)' DEFAULT 0, `debtSueTotalDebtAmount` decimal(10,2) NOT NULL COMMENT 'ยอดหนี้รวมทั้งหมด' DEFAULT 0, `debtSueJudgementInterestStartDate` date NULL COMMENT 'วันที่เริ่มคิดดอกเบี๊ย', `debtSueJudgementInterestEndDate` date NULL COMMENT 'วันที่สิ้นสุดการคิดดอกเบี๊ย', `debtSueJudgementInterestRate` decimal(10,2) NOT NULL COMMENT 'ดอกเบี้ยตามคำสั่งศาล (%)' DEFAULT 0, `debtSueJudgementInterestAmount` decimal(10,2) NOT NULL COMMENT 'ดอกเบี้ยตามคำสั่งศาล' DEFAULT 0, `debtSueJudgementBalance` decimal(10,2) NOT NULL COMMENT 'หนี้ตามคำพิพากษา' DEFAULT 0, `debtSueJudgementTotalDebtAmount` decimal(10,2) NOT NULL COMMENT 'ยอดหนี้รวมตามคำพิพากษา' DEFAULT 0, `debtSueLawyerFee` decimal(10,2) NOT NULL COMMENT 'ค่าทนาย' DEFAULT 0, `debtSueFee` decimal(10,2) NOT NULL COMMENT 'ค่าฤชาธรรมเนียม' DEFAULT 0, `debtSueOtherExpense` decimal(10,2) NOT NULL COMMENT 'ค่าใช้จ่ายอื่นๆ' DEFAULT 0, `debtSueTotalAmount` decimal(10,2) NOT NULL COMMENT 'ยอดรวมทั้งหมดจากการดำเนินคดี' DEFAULT 0, `deathNotificationIsConfirm` tinyint NOT NULL COMMENT 'ยืนยันการเสียชีวิต' DEFAULT 0, `deathNotificationNotificationDate` date NULL COMMENT 'วันที่ได้รับการ', `deathNotificationName` varchar(8) NOT NULL COMMENT 'รหัสตำบล' DEFAULT '', `deathNotificationPosition` varchar(128) NOT NULL COMMENT 'ตำบล' DEFAULT '', PRIMARY KEY (`id`)) ENGINE=InnoDB"
    );
    await queryRunner.query(
      "CREATE TABLE `memos` (`id` bigint NOT NULL AUTO_INCREMENT, `createdDate` datetime(6) NULL COMMENT 'วันที่สร้าง' DEFAULT CURRENT_TIMESTAMP(6), `updatedDate` datetime(6) NULL COMMENT 'วันที่ปรับปรุง' DEFAULT CURRENT_TIMESTAMP(6), `createdBy` int NULL COMMENT 'รหัสผู้ใช้ที่สร้าง', `createdByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่สร้าง' DEFAULT '', `updatedBy` int NULL COMMENT 'รหัสผู้ใช้ที่ปรับปรุงล่าสุด', `updatedByName` varchar(255) NOT NULL COMMENT 'ชื่อผู้ใช้ที่ปรับปรุงล่าสุด' DEFAULT '', `refId` int NULL, `refType` varchar(255) NULL, `location` varchar(255) NOT NULL COMMENT 'สถานที่จดบันทึก ดึงข้อมูลอัตโนมัตจาก organization' DEFAULT '', `documentDate` date NULL COMMENT 'วันที่จดบันทึก', `documentTime` time NULL COMMENT 'เวลาที่จดบันทึก', `memoInformer` varchar(255) NULL COMMENT 'ประเภท ผู้ขอกู้ = \"B\", ผู้ค้ำ = \"G\", ผู้ทราบข้อมูลผู้กู้ = \"BW\" , ผู้ทราบข้อมูลผู้ค้ำ = \"GW\" ', `memoInformerRelationship` int NOT NULL COMMENT 'ความสัมพันธ์ของผู้ให้ข้อมูลกับผู้กู้ผู้ค้ำ(กรณีที่ไม่ใช่เจ้าตัว) children = 0,relative = 1, friend = 2,', `title` varchar(128) NOT NULL COMMENT 'คำนำหน้าชื่อ' DEFAULT '', `firstname` varchar(128) NOT NULL COMMENT 'ชื่อ' DEFAULT '', `lastname` varchar(128) NOT NULL COMMENT 'นามสกุล' DEFAULT '', `age` int NOT NULL COMMENT 'อายุ', `birthDate` date NULL COMMENT 'วันเกิด >> หากไม่ทราบวันเกิดจะระบุเป็นวันที่  1/1/YYYY', `isOnlyBirthYear` tinyint NOT NULL COMMENT 'ไม่ทราบวันเกิด' DEFAULT 0, `isWorking` tinyint NOT NULL COMMENT 'สถานะการประกอบอาชีพ  ทำงาน/ไม่ทำงาน', `mobilePhone` varchar(64) NOT NULL COMMENT 'หมายเลขโทรศัพท์มือถือ' DEFAULT '', `memoTitle` varchar(256) NOT NULL COMMENT 'หัวข้อบันทึก' DEFAULT '', `memoNote` varchar(512) NOT NULL COMMENT 'ข้อความที่ให้ถ้อยคำ' DEFAULT '', `interviewerName` varchar(255) NOT NULL COMMENT 'ชื่อผู้บันทึก' DEFAULT '', `interviewerPosition` varchar(255) NOT NULL COMMENT 'ตำแหน่งผู้บันทึก' DEFAULT '', `comments` varchar(255) NOT NULL COMMENT 'เหตุผลประกอบ' DEFAULT '', `occupationId` varchar(255) NULL COMMENT 'รหัสอาชีพ', `occupationName` varchar(128) NOT NULL COMMENT ' ชื่ออาชีพ' DEFAULT '', `occupationDescription` varchar(128) NOT NULL COMMENT 'คำอธิบาย' DEFAULT '', `occupationSalary` decimal(10,2) NOT NULL COMMENT 'เงินเดือน' DEFAULT 0, `currentAddressHouseNo` varchar(128) NOT NULL COMMENT 'เลขที่บ้าน' DEFAULT '', `currentAddressBuildingName` varchar(128) NOT NULL COMMENT 'ชื่ออาคาร หมู่บ้าน' DEFAULT '', `currentAddressRoomNo` varchar(128) NOT NULL COMMENT 'ห้อง' DEFAULT '', `currentAddressFloor` varchar(128) NOT NULL COMMENT 'ชั้น' DEFAULT '', `currentAddressHmoo` varchar(128) NOT NULL COMMENT 'หมู่ที่' DEFAULT '', `currentAddressSoi` varchar(128) NOT NULL COMMENT 'ซอย' DEFAULT '', `currentAddressStreet` varchar(128) NOT NULL COMMENT 'ถนน' DEFAULT '', `currentAddressSubDistrictCode` varchar(8) NOT NULL COMMENT 'รหัสตำบล' DEFAULT '', `currentAddressSubDistrict` varchar(128) NOT NULL COMMENT 'ตำบล' DEFAULT '', `currentAddressDistrictCode` varchar(8) NOT NULL COMMENT 'รหัสอำเภอ' DEFAULT '', `currentAddressDistrict` varchar(128) NOT NULL COMMENT 'อำเภอ' DEFAULT '', `currentAddressProvinceCode` varchar(8) NOT NULL COMMENT 'รหัสจังหวัด' DEFAULT '', `currentAddressProvince` varchar(128) NOT NULL COMMENT 'จังหวัด' DEFAULT '', `currentAddressZipcode` varchar(8) NOT NULL COMMENT 'รหัสไปรษณีย์' DEFAULT '', `currentAddressLatitude` varchar(128) NOT NULL COMMENT 'ละติจูด' DEFAULT '', `currentAddressLongitude` varchar(128) NOT NULL COMMENT 'ลองจิจูด' DEFAULT '', PRIMARY KEY (`id`)) ENGINE=InnoDB"
    );
    await queryRunner.query(
      "ALTER TABLE `account_receivables` DROP COLUMN `paidMonthCounts`"
    );
    await queryRunner.query(
      "ALTER TABLE `account_receivables` ADD `unpaidMonthCountsInArow` int NOT NULL COMMENT 'จำนวนเดือนที่ขาดการชำระโดยต่อเนื่อง ถ้ามีการชำระจะถูก reset เป็น 0 เสมอ' DEFAULT 0 COMMENT 'จำนวนเดือนที่ขาดการชำระโดยต่อเนื่อง ถ้ามีการชำระจะถูก reset เป็น 0 เสมอ' "
    );
    await queryRunner.query(
      "ALTER TABLE `account_receivables` ADD `paidPercentage` decimal(10,2) NOT NULL COMMENT 'สัดส่วนการชำระหนี้ ว่าชำหนี้ทั้งหมดมาแล้วกี่% = (LoanAmount - outstandingDebtBalance)/LoanAmount' DEFAULT 0 COMMENT 'สัดส่วนการชำระหนี้ ว่าชำหนี้ทั้งหมดมาแล้วกี่% = (LoanAmount - outstandingDebtBalance)/LoanAmount' "
    );
    await queryRunner.query(
      "ALTER TABLE `debt_collection_letters` ADD CONSTRAINT `FK_fc9520e236474f9b2a216121242` FOREIGN KEY (`debtCollectionId`) REFERENCES `debt_collections`(`id`) ON DELETE CASCADE ON UPDATE NO ACTION"
    );
    await queryRunner.query(
      "ALTER TABLE `debt_collection_visits` ADD CONSTRAINT `FK_0d700b101a1f0927ae5660a8248` FOREIGN KEY (`debtCollectionId`) REFERENCES `debt_collections`(`id`) ON DELETE CASCADE ON UPDATE NO ACTION"
    );
    await queryRunner.query(
      "ALTER TABLE `debt_collections` ADD CONSTRAINT `FK_e5641c71f5dbf185cea5c9d37d0` FOREIGN KEY (`accountReceivableId`) REFERENCES `account_receivables`(`id`) ON DELETE CASCADE ON UPDATE NO ACTION"
    );
  }

  public async down(queryRunner: QueryRunner): Promise<any> {
    await queryRunner.query(
      "ALTER TABLE `debt_collections` DROP FOREIGN KEY `FK_e5641c71f5dbf185cea5c9d37d0`"
    );
    await queryRunner.query(
      "ALTER TABLE `debt_collection_visits` DROP FOREIGN KEY `FK_0d700b101a1f0927ae5660a8248`"
    );
    await queryRunner.query(
      "ALTER TABLE `debt_collection_letters` DROP FOREIGN KEY `FK_fc9520e236474f9b2a216121242`"
    );
    await queryRunner.query(
      "ALTER TABLE `account_receivables` DROP COLUMN `paidPercentage`"
    );
    await queryRunner.query(
      "ALTER TABLE `account_receivables` DROP COLUMN `unpaidMonthCountsInArow`"
    );
    await queryRunner.query(
      "ALTER TABLE `account_receivables` ADD `paidMonthCounts` int NOT NULL COMMENT 'จำนวนครั้งที่จ่าย นับตามกฎหมายนับว่าจ่ายไปแล้วกี่เดือน' DEFAULT '0'"
    );
    await queryRunner.query("DROP TABLE `memos`");
    await queryRunner.query("DROP TABLE `debt_collections`");
    await queryRunner.query("DROP TABLE `debt_collection_visits`");
    await queryRunner.query("DROP TABLE `debt_collection_letters`");
  }
}
